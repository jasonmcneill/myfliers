# Core API (Library Catalog Service)

This is the central "Library Catalog" API for the MyFliers project. It is a
"headless" service built in Node.js, TypeScript, and Docker.

## Project Structure

`/api-design`: Contains the Bruno API contract. This is the "blueprint."

`/src`: The TypeScript source code for the API.

`package.json`: Project dependencies.
